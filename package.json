{
  "name": "workout-pwa",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "concurrently \"node build.js --watch\" \"serve dist --listen 3000\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.20.0",
    "serve": "^14.2.6",
    "typescript": "^5.4.0"
  }
}
